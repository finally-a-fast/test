# phpstorm project files
.idea

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# sublime text project / workspace files
*.sublime-project
*.sublime-workspace

# gitpod
.theia

# windows thumbnail cache
Thumbs.db

# Mac DS_Store Files
.DS_Store

# sass cache
.sass-cache

# composer vendor dir
/vendor

# composer itself is not needed
composer.phar

# composer.lock in applications is ignored since it's automatically created by composer when application is installed
composer.lock

# phpunit itself is not needed
phpunit.phar

# local infection config
infection.json

# local phpunit watcher config
.phpunit-watcher.yml
phpunit-watcher.yml

# local phpunit config
phpunit.xml

# phpunit cache
.phpunit.result.cache

# log files
/examples/parser.log
infection.log
infection-summary.log
